RSpec.describe 'Docker' do
    it 'helps us eliminate differences between CI and dev environments' do
        expect('indeed it does').to be
    end
    it 'Can run more tests on commit' do
        expect('indeed it does').to be
    end
end
